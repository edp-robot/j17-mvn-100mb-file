apiVersion: v1
kind: Service
metadata:
  name: {{ include "j17-mvn-100mb-file.fullname" . }}
  labels:
    {{- include "j17-mvn-100mb-file.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "j17-mvn-100mb-file.selectorLabels" . | nindent 4 }}
